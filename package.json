{
  "name": "komorebi-cat-cafe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.js",
    "check": "npm test && npm run build",
    "test:scene:prepare": "node scripts/prepare-scene-tests.mjs"
  },
  "dependencies": {
    "lucide": "^1.41.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
